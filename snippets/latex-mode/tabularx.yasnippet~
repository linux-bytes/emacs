# -*- mode: snippet -*-
# name: tabularx
# key: 
# --
    \begin{tabularx}{\textwidth}{p{3cm}X}
      \toprule
      \textbf{Member Name}& \textbf{Function} \\
      \toprule
      irq      & 若 $irq < 0$, 对分配出来的中断号没有要求; 若 $irq >= 0$, 如果分配出来中断号 (若试图分配多个中断号，那么起始中断号) 不等于 irq, 则会返回 \cc{-EEXIST}.\\
      \midrule
      from     &  从 from 开始寻找可用的中断号. \\
      \midrule
      cnt      &  需要分配多少个中断号. \\
%      \midrule
%      node, owner, affinity &  暂时不解释. \\
      \bottomrule
    \end{tabularx}